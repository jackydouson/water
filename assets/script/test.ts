const {ccclass, property} = cc._decorator;

@ccclass
export default class Cursor extends cc.Component {
    onLoad () {
        //this.node.runAction(cc.sequence(cc.fadeIn(1), cc.fadeOut(1)).repeatForever())
    }
}